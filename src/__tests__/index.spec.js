import {
  changeStringCase,
  sumOfArray,
  divisibleBy,
  addDotsToString,
  objectEntries,
} from '../index';

describe('changeStringCase()', () => {});

describe('sumOfArray()', () => {});

describe('divisibleBy()', () => {});

describe('addDotsToString()', () => {});

describe('objectEntries()', () => {});
